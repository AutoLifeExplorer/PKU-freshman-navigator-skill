# 验收记录

验收时间：2026-08-28（北京时间）

## 包结构

- `scripts/validate_package.py`：通过。
- Skill Creator `quick_validate.py`：通过，结果为 `Skill is valid!`。
- 固定行为测试：12 个案例已建立，覆盖大纲中的 6 个核心提问和 6 个边界场景。
- HTML 样例：3 个，均为单文件自包含，无外部脚本、样式、字体或浏览器存储。

## 官方入口实时检查

在允许联网的本地环境运行 `scripts/check_live_sources.py`：8 / 8 通过，均返回 HTTP 200。

通过入口：

1. 北京大学教务部首页。
2. 教务部信息下载。
3. 教务部学生服务。
4. 北京大学信息公开网学生管理。
5. 北京大学图书馆首页。
6. 图书馆服务时间与空间布局。
7. 北京大学计算中心。
8. 正版软件说明。

该结果只代表本次检查时可访问。Skill 运行时仍须实时核验，不得把本记录当作永久状态。

## 网页端视觉与交互

使用本地浏览器分别检查 1440 × 1000 与 390 × 844 视口：

| 样例 | 桌面 | 窄屏 | 交互 |
| --- | --- | --- | --- |
| 自习地点三选一 | 通过 | 通过，无横向溢出 | 筛选后可见卡片数正确；复制按钮有状态反馈 |
| 免费资源解锁 | 通过 | 通过，无横向溢出 | 分类筛选正确；勾选后完成度从 0/4 更新为 1/4 |
| 四年雷达 | 通过 | 通过，无横向溢出 | 阶段筛选正确；日历生成按钮运行且无控制台错误 |

三个页面均确认：

- 存在明确的非官方标记、学年和核验时间。
- 核心内容不依赖 JavaScript 才能阅读。
- 使用语义化按钮、链接、复选框和 `details/summary`。
- 支持 `prefers-reduced-motion`，并提供键盘焦点样式。
- 320px 以上布局规则为单列；390px 实测无横向滚动。

## 日历导出

四年雷达样例的日历内容通过静态完整性检查，包含 `VCALENDAR` 起止标记、亚洲/上海时区日期和官方来源 URL。示例只导出官方已明确的选课系统关闭时间；观察窗口不生成虚假截止日期。

## 已知边界

- 固定行为案例尚需在目标千问办公账号中逐条运行，才能评估实际模型路由与联网检索表现；当前记录不冒充目标平台端到端结果。
- 校园地点的插座、拥挤、准入和营业信息变化快。经验种子中对旧信息和待核验项已显式降级。
- 不依赖网页端独占功能；桌面端应使用 `SKILL.md` 规定的完整纯文本输出。最终安装后仍应在两个客户端各做一次冒烟测试。

