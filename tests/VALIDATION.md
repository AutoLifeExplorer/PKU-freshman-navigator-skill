# V2 验收记录

验收时间：2026-08-31（北京时间）

## 结构与指令

- `scripts/validate_package.py`：通过。
- Skill Creator `quick_validate.py`：通过，结果为 `Skill is valid!`。
- 固定行为案例：12 个，均包含路由、检索、形式选择、追问策略和场景不变量。
- 语言风格已调整为自然、热情、有生活感的北大学长学姐口吻；简单问题简答，复杂问题充分展开。
- 已移除固定字数、步骤数和视觉单元限制，改为按问题复杂度分配篇幅、按信息重要性分层。
- 固定免责声明和机械兜底句要求已移除；事实核验与隐私边界保留。

## 官方入口实时检查

在允许联网的环境运行 `scripts/check_live_sources.py`：8 / 8 通过，均返回 HTTP 200。

通过入口：教务部首页、信息下载、学生服务、学生管理信息公开、图书馆首页、图书馆空间布局、计算中心、正版软件说明。

该结果只代表本次检查时可访问。Skill 运行时仍须核验当前内容、适用范围和发布日期。

## 四类页面

| 页面 | 独立结构 | 已验证交互 |
| --- | --- | --- |
| 地点排序路径 | 硬条件 → 首选 → 条件备选 | 条件切换会改写首选与路径节点；复制按钮有反馈 |
| 资源解锁面板 | 未开通 → 下一枚章 → 已解锁 | 勾选后进度与下一项同步更新 |
| 制度条件流程 | 官方结论 → 院系分支 → 当前动作 | 细则状态会改写动作、结论和变更条件 |
| 四年行动时间轴 | 现在 → 下一窗口 → 以后 | 阶段切换正常；完整时间轴展开；日历导出可触发 |

## 浏览器实测

- 1440 × 1000：四页结构清晰，无横向溢出。
- 390 × 844：四页移动端重排正常，核心交互有效。
- 320 × 700：四页横向溢出均为 0；首选和主行动均位于首屏。
- 浏览器控制台：交互测试期间无 JavaScript 错误。
- 页面均包含键盘焦点、触控尺寸、`prefers-reduced-motion`、`noscript` 核心答案和带文字的证据状态。
- 页面无远程脚本、样式、字体、统计或浏览器存储。

## 日历导出

四年时间轴包含完整 `VCALENDAR` 起止标记、亚洲／上海时区日期和官方来源 URL。样例明确标记为演示日期；运行时只有官方已核实的精确日期可以导出。

## 仍需目标平台验证

仓库没有千问办公端到端运行器。12 个行为案例已完成结构和不变量校验，但安装后的实际模型表现仍需在目标账号中逐条冒烟测试；本记录不把静态验收冒充平台实测。
