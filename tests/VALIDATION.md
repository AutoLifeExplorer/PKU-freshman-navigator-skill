# V3 验收记录

验收时间：2026-08-31（北京时间）

## 结构与指令

- `scripts/validate_package.py`：通过。
- Skill Creator `quick_validate.py`：通过，结果为 `Skill is valid!`。
- 固定行为案例：26 个，覆盖 12 条路由、8 类输出引擎和低／中／高三级风险。
- Skill 入口已改为按任务结构选择一个主输出，并按需加载核心旅程或校园服务手册。
- 新增 V3 任务对象、核验模式、门户最小授权、分享隔离和高风险禁止操作边界。
- 现有来源政策扩展为官方事实、体验观察和个人数据三轨。

行为案例当前完成的是结构与不变量覆盖；仓库没有目标平台模型运行器，未把静态案例检查冒充真实模型逐条运行。

## 官方入口实时检查

`scripts/check_live_sources.py` 检查 15 个官方入口：

- 13 个由 Python 检查直接返回 HTTP 200。
- 保卫部与学生资助中心在 Python 网络栈中出现连接错误。
- 使用独立 HTTP 响应检查复核上述两个入口，均返回 HTTP 200。

因此 15 个注册入口在本次验收时均可访问，但运行时仍须检查当前页面内容、适用范围、发布日期和附件版本。连接错误必须触发核验模式，不能当作事实缺失后自行补全。

## 五个页面

| 页面 | 主结构 | 已验证交互 |
| --- | --- | --- |
| 报到准备工作台 | 对话 + 时间工作台 + 当前清单 | 清单推进、日历预览、空选择阻止、单项导出状态、脱敏分享、操作记录 |
| 地点排序路径 | 硬条件 → 首选 → 条件备选 | 切换“需要讨论”后首选更新为和声厅 |
| 资源解锁面板 | 未开通 → 当前项 → 已完成 | 勾选后完成数和下一资源同步更新 |
| 制度条件流程 | 结论 → 适用分支 → 当前动作 | 找到细则后动作更新为核对适用年级与评价维度 |
| 四年行动时间轴 | 现在 → 下一窗口 → 以后 | 阶段切换正常，当前与下一节点显隐正确 |

浏览器控制台在上述交互期间无 JavaScript 错误或警告。

## 电脑端 V3 工作台

- 1440 × 1000：对话区约 32%，工作台约 68%；无横向溢出，主行动可见。
- 工作台顶部只显示加入日历、生成分享版和复制摘要三个情境操作。
- 路径脊柱实际编码当前、下一窗口与以后。
- 勾选“保存开学节点”后，进度由 1/4 更新为 2/4，下一动作更新为测试门户与邮箱。
- 日历预览支持逐项取消；全部取消时阻止导出；单项选择后生成 VCalendar 内容并记录撤销说明。
- 分享预览明确列出已移除的学号、宿舍房间、个人课表、起点和私人备注。
- 演示页未连接真实门户，并明确说明不接触密码。

## 响应式与无障碍

5 个页面分别在 1440 × 1000、390 × 844 和 320 × 700 检查，共 15 个视口组合：

- 横向溢出全部为 0。
- 页面均存在明确主结论和主行动。
- V3 工作台在 390px 将对话压缩为变化摘要，并提前显示当前行动。
- 320px 下移动主行动位于首屏，所有可见按钮高度至少 44px，任务标签点击区域约 74px。
- 页面均包含键盘焦点样式、语义控件、`prefers-reduced-motion` 和 `noscript` 文本等价内容。
- 状态同时使用符号与文字，不只靠颜色。
- 页面无远程脚本、远程样式、统计或浏览器存储。

浏览器自动化对原生对话框完成了语义控件与焦点目标检查；完整屏幕阅读器朗读仍需在目标系统中人工验收。

## 日历与导出

- 报到工作台和四年时间轴均包含完整 `VCALENDAR` 起止标记、唯一事件 ID、日期／时区和官方 URL。
- 报到工作台的日历写入先预览，且只包含样例中已核实的精确日期。
- 导出文件是带生成时间与适用范围的快照，不承诺自动更新。
- Word、Excel、PDF、地图、微信和邮件目前定义了契约与首期边界；本静态 Skill 包没有可供端到端调用的真实连接器。

## 尚需目标平台验证

1. 北大统一身份认证允许的合法接入方式、字段、会话和授权范围。
2. 课表、培养方案和办理状态是否存在可读取的结构化接口。
3. Office/WPS、地图、微信和邮件在目标宿主中的真实打开与导出行为。
4. 安装后的实际模型是否在 26 个行为案例中稳定选择正确引擎并遵守高风险边界。
5. 屏幕阅读器、系统高对比度和真实文件回流差异确认。
